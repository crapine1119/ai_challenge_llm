class CompanyAnalysisService:
    def extract_knowledge(self, document):
        """
        :param document:
        :return:
        """
        """
        job_categories = get_job_category
        # 인재상 추출 (require simple search?)
        await extract_company_qualities(document) 
        
        # 직무별 역량 추출
        for job_category in job_categories:
            await extract_job_competency(document, category) 
             
        # 저장
        await update_company_knowledge
        
        """

    def build_template(self): ...